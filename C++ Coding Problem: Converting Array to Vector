#include <iostream>
#include <vector>

using namespace std;

int main()
{
    vector<int> array = {1 ,2, 3, 4, 5};
    /*
    for(int i = 0; i < sizeof(array)/sizeof(array[0]); i++){
        std::cout << array[i] << " ";
    }
    */
    cout << sizeof(array[5]);

    return 0;
}
